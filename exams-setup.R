install.packages("exams")
install.packages("exams2forms")

# exams::exams_skeleton(
#   dir = "exams",
#   markup = "markdown",
#   writer = c("exams2html")
# ) #installs example exercises

#download source code and put webex.css and webex.js in the project root directory

# add to _quarto.yml:
# format:
#   html:
#     css: webex.css
#     include-after-body: webex.js
